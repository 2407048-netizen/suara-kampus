@"
{
  "version": 2,
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ]
}
"@ | Set-Content vercel.json -Encoding UTF8

Write-Host "✅ vercel.json sudah diperbaiki" -ForegroundColor Green